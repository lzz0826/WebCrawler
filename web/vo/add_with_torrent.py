

class Add_With_Torrent:
    def __init__(self, source, article_id, title, content, tags, source_url, folder_path, file_name):
        self.source = source
        self.article_id = article_id
        self.title = title
        self.content = content
        self.tags = tags
        self.source_url = source_url
        self.folder_path = folder_path
        self.file_name = file_name